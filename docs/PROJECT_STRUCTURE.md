# Project Structure

The GameMaker project is stored as a complete project folder under `game/Surface of Abbyss/`.

Internal folders such as `objects`, `sprites`, `sounds`, `rooms`, and `datafiles` were not split out because the `.yyp` file resolves them as project-relative resources. Keeping them together preserves GameMaker compatibility while still giving the repository a clearer top-level layout.
